{
  "version": 2,
  "builds": [
    {
      "src": "server/**/*.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/server/$1.js"
    }
  ],
  "functions": {
    "server/**/*.js": {
      "maxDuration": 60
    }
  }
}
